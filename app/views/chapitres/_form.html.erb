<%= tinymce %>
<%= simple_form_for(@chapitre) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
   <%= f.text_area :text, :class => "tinymce", :rows => 20, :cols => 60 %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
